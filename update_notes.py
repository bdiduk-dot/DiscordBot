from __future__ import annotations

import discord

from config import COLORS

LATEST_UPDATE_ID = "2026-04-02-upd-2-global-rebalance"
UPDATE_PING_ROLE_ID = 1486391112330379304
UPDATE_CHANNEL_ID = 1486032811809964243
UPDATE_TITLE = "🌐 Глобальное обновление 2.0"

LATEST_UPDATE_TEXT = """
> Большой этап 2.0 уже в боте. Прогресс игроков сохранён, а ключевые системы стали заметно чище, понятнее и удобнее в использовании.

## 🏠 Дома и огороды
▸ Публичные квартиры полностью убраны: теперь везде показываются только дома
▸ Квартиры стали полноценными домами без сброса прогресса
▸ Подвалы, GPU, аренда, мебель, сад и старый майнинг-кошелёк сохранены без потери прогресса
▸ `/house` стал главным экраном дома с вкладками `Дом`, `Сад`, `Крипта`, `Аренда`, `Обустройство`
▸ Огород теперь зависит от типа дома: от 6 до 16 грядок
▸ Семена покупаются в `/shop`, рост культур ставится на паузу без полива
▸ Урожай попадает в инвентарь как предмет, а не конвертируется сразу в деньги
▸ Лейки влияют на интервал полива и улучшают обслуживание сада
▸ В саду теперь используется понятный статус `полить через`, а сухие грядки явно помечаются как требующие полива

## ₿ Крипта
▸ Подвал больше не фармит доллары напрямую, только криптовалюту
▸ Хэшрейт конвертируется в монеты по живому рынку через взвешенный ролл
▸ Старый денежный остаток из майнинга вынесен в отдельный кошелёк для безопасного вывода
▸ Вся крипта управляется через вкладку `Крипта` в `/house`
▸ Во вкладке доступны сбор, продажа всей крипты, продажа по отдельной монете и вывод старого кошелька
▸ Крипта живёт внутри `/house`: сбор, продажа, фокус монеты и вывод старого кошелька теперь в одном месте
▸ Новые GPU покупаются через `/shop` → `Недвижимость`, а не из вкладки крипты
▸ Вкладка `Крипта` упрощена и стала понятнее, а фокус монеты помогает настроить майнинг без потери случайности

## 🎣 Сад, рыбалка и инвентарь
▸ Семена покупаются в `/shop`, а урожай падает в инвентарь предметами
▸ В `/inventory` снова доступны три вкладки: `🎒 Предметы`, `🐟 Рыба`, `🎣 Снаряжение`
▸ Переключение между `Предметы`, `Рыба` и `Снаряжение` снова полностью рабочее
▸ Без наживки шанс легендарной рыбы равен `0%`
▸ С наживкой списывается ровно 1 штука за заброс и растёт шанс на ценную рыбу
▸ Добавлен `Затонувший сундук` с шансом `0.5%`
▸ Крипта из сундука идёт в кошелёк дома, а без дома падает в инвентарь как предмет

## 🎨 Интерфейсы и экономика
▸ `/profile` стал компактнее и чище, а внутри появились read-only кнопки `Дом` и `Бизнесы`
▸ `/bank @player` открывает чужой банк в режиме только чтения с полной депозитной сводкой
▸ `/top` считает капитал по деньгам, банку, депозиту, домам, подвалам, GPU, бизнесам и мебели
▸ `/timers` переложен в компактный dashboard со статусами по экономике, активностям, рыбалке, бизнесам, дому и сбросам
▸ `/businesses` использует нормальные Discord-таймеры вместо ASCII-загрузчиков
▸ `/shop` теперь единый: `Главное`, `Недвижимость`, `Рыбалка`, `Садовод`, `ИКЕА`

## 🛍 Магазин
▸ `/houseshop` и `/fishshop` удалены, теперь используется единый `/shop`
▸ В `/shop` появились категории `Главное`, `Недвижимость`, `Рыбалка`, `Садовод`, `ИКЕА`
▸ Раздел `Главное` открывается первым и содержит VIP, обмен, пропуск, титулы и серверные товары

## 🏆 Лидерборд и стрики
▸ `/leaderboard` удалён, остался только `/top`
▸ Net Worth считается по балансу, банку, депозиту, дому, подвалу, GPU, бизнесам и мебели
▸ Рыба, обычный инвентарь и крипта в рейтинг не входят
▸ `/daily` теперь держит серию 48 часов, а каждый 7-й день удваивает денежную часть награды

## 🛋️ Мебель и декор
▸ В `/shop` появилась категория `ИКЕА`
▸ Стартовый набор: `Геймерское кресло`, `Аквариум`, `Плазменный ТВ`
▸ Баффы от мебели работают только у владельца дома на текущем сервере
▸ Детали по обустройству доступны во вкладке `Обустройство` в `/house`

## 💼 Работа и криминал
▸ `/work` теперь предлагает 3 безопасные работы с гарантированным успехом
▸ `/crime` предлагает 3 рисковых варианта с шансом, кушем и штрафом
▸ `/slut` получил большой пул абсурдных исходов с высокой дисперсией
▸ `/crime`, `/slut` и `/steal` теперь прямо показывают, когда теневая страховка спасла от штрафа и не сняла репутацию
▸ В `/steal` и других рискованных сценариях теневая страховка теперь тоже показывает понятный нулевой штраф, если защита сработала

## 🏦 Банк 2.0
▸ Старые команды `/bankdep`, `/bankwith`, `/deposit`, `/withdraw_deposit` удалены
▸ Весь банк собран в одном `/bank`
▸ На экране банка видны наличные, счёт, депозит, процент, время до созревания и штраф за досрочный вывод
▸ Ставки депозита: **1 день** → `+2%` · **3 дня** → `+7%` · **7 дней** → `+20%`

## 🛡 Риск и защита
▸ Теневая страховка из чёрного маркета снова проходит полный цикл: покупка, активация через инвентарь и защита в рискованных сценариях
▸ Аренда получила случайные события с бонусами и штрафами

## fastFIX
▸ В `/profile` добавлены кнопки `Дом` и `Бизнесы` с read-only экранами для быстрого просмотра
▸ `/bank @player` теперь открывает чужой банк в режиме **только чтение** вместе с депозитной сводкой
▸ Временные игровые и UI-сообщения теперь удаляются после **2 минут неактива**, а завершённые экраны не висят лишнее время
▸ Блэкджек, дуэли, банк, дом, магазин, инвентарь и другие временные меню после 2 минут неактива автоматически исчезают
▸ `/timers` получил более чистый dashboard-вид с короткими статусами по экономике, активностям, рыбалке, бизнесам, дому и сбросам
▸ В `/inventory` возвращена полноценная вкладка `🎣 Снаряжение`
▸ Публичные квартиры переименованы в дома без потери прогресса: все подвалы, GPU, аренда и накопления сохранены
▸ Вкладка `Крипта` в `/house` упрощена: добавлен понятный фокус монеты, убрана покупка GPU из этого экрана, а сами GPU покупаются через `/shop` → `Недвижимость`
▸ После каждого рестарта бот публикует новое сообщение с обновлением в канал updates, удаляет старое сообщение и закрепляет новое
"""


def _split_update_text(text: str, limit: int = 3800) -> list[str]:
    lines = text.strip().splitlines()
    chunks: list[str] = []
    current: list[str] = []
    current_len = 0

    for line in lines:
        extra = len(line) + (1 if current else 0)
        if current and current_len + extra > limit:
            chunks.append("\n".join(current).strip())
            current = [line]
            current_len = len(line)
        else:
            current.append(line)
            current_len += extra

    if current:
        chunks.append("\n".join(current).strip())
    return chunks or [text[:limit]]


def build_update_embeds() -> list[discord.Embed]:
    chunks = _split_update_text(LATEST_UPDATE_TEXT)
    embeds: list[discord.Embed] = []

    for index, chunk in enumerate(chunks, start=1):
        title = UPDATE_TITLE if index == 1 else f"{UPDATE_TITLE} • продолжение {index}/{len(chunks)}"
        embed = discord.Embed(
            title=title,
            description=chunk,
            color=COLORS["info"],
        )
        embed.set_footer(text=f"Update ID: {LATEST_UPDATE_ID}")
        embeds.append(embed)

    return embeds


def build_update_embed() -> discord.Embed:
    return build_update_embeds()[0]
