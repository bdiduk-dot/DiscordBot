from __future__ import annotations

import discord

from config import COLORS

LATEST_UPDATE_ID = "2026-04-03-upd-2-1-notifications-blackjack"
UPDATE_PING_ROLE_ID = 1486391112330379304
UPDATE_CHANNEL_ID = 1486032811809964243
UPDATE_TITLE = "🌐 Глобальное обновление 2.0"

LATEST_UPDATE_TEXT = """
> Большой этап 2.0 уже в боте. Прогресс игроков сохранён, а ключевые системы стали заметно чище, понятнее и удобнее в использовании.

## 🏠 Дома и огороды
▸ Квартиры стали полноценными домами без сброса прогресса
▸ `/house` — главный экран с вкладками `Дом` `Сад` `Крипта` `Аренда` `Обустройство`
▸ Огород зависит от типа дома: от 6 до 16 грядок, рост без полива ставится на паузу
▸ Урожай падает в инвентарь как предмет

## ₿ Крипта
▸ Подвал больше не фармит доллары — только криптовалюта
▸ Всё управление криптой в одном месте: вкладка `Крипта` в `/house`
▸ Старый денежный остаток вынесен в отдельный кошелёк для безопасного вывода
▸ Продажа по живому рынку, а не по фиксированным курсам

## 🎣 Рыбалка и инвентарь
▸ Без наживки шанс легендарной рыбы = `0%`
▸ Добавлен **Затонувший сундук** с шансом `0.5%`
▸ `/inventory` — три вкладки: `🎒 Предметы` `🐟 Рыба` `🎣 Снаряжение`

## 🛍 Магазин
▸ `/houseshop` и `/fishshop` удалены — остался единый `/shop`
▸ Категории: `Главное` `Недвижимость` `Рыбалка` `Садовод` `ИКЕА`
▸ `Главное` открывается первым: VIP, обмен, пропуск, титулы, серверные товары

## 🏆 Лидерборд и стрики
▸ `/leaderboard` удалён — только `/top`
▸ Net Worth учитывает баланс, банк, депозит, дом, подвал, GPU, бизнесы и мебель
▸ `/daily` держит серию 48 часов, каждый 7-й день удваивает денежную часть

## 🛋️ Мебель, работа и декор
▸ Категория `ИКЕА` в `/shop` — стартовый набор с реальными баффами
▸ `/work` — гарантированный доход · `/crime` — риск и куш · `/slut` — высокая дисперсия

## 🏦 Банк 2.0
▸ Все старые банковские команды удалены — только `/bank`
▸ Один экран: наличные, счёт, депозит, процент, таймер, штраф за досрочный вывод
▸ Ставки: **1 день** `+2%` · **3 дня** `+7%` · **7 дней** `+20%`

─────────────────────────────────────
**📦 Патч 2.1** — *небольшое обновление*
─────────────────────────────────────

## 🔔 Умные уведомления
▸ Бот сам напишет в личку, когда что-то требует внимания:
　— ✅ Депозит созрел и готов к получению
　— 🏠 Аренда собрана и ждёт тебя
　— 🏢 Бизнес принёс доход
　— 🌱 Урожай готов к сбору
　— ⚠️ Daily streak почти сгорает
▸ Управление уведомлениями — в `/settings`

## ⚙️ Настройки — `/settings`
▸ Новая команда для управления персональными настройками бота
▸ Уведомления отключаются **точечно** — каждый тип отдельно:
　— `Депозит` · `Аренда` · `Бизнес` · `Урожай` · `Daily streak`
▸ Можно убрать с себя роль обновлений и отказаться от её автовыдачи

## 🃏 Обновленный Блэкджек
▸ Интерфейс остался простым — механики стали настоящими:
　— **Удвоить** — ставка ×2, один финальный карточный добор
　— **Сплит** — разбить пару на два независимых стола *(если карты одинаковые)*
　— **Страховка** — защита на половину ставки, если у дилера туз
"""


def _split_update_text(text: str, limit: int = 3800) -> list[str]:
    lines = text.strip().splitlines()
    chunks: list[str] = []
    current: list[str] = []
    current_len = 0

    for line in lines:
        extra = len(line) + (1 if current else 0)
        if current and current_len + extra > limit:
            chunks.append("\n".join(current).strip())
            current = [line]
            current_len = len(line)
        else:
            current.append(line)
            current_len += extra

    if current:
        chunks.append("\n".join(current).strip())
    return chunks or [text[:limit]]


def build_update_embeds() -> list[discord.Embed]:
    chunks = _split_update_text(LATEST_UPDATE_TEXT)
    embeds: list[discord.Embed] = []

    for index, chunk in enumerate(chunks, start=1):
        title = UPDATE_TITLE if index == 1 else f"{UPDATE_TITLE} • продолжение {index}/{len(chunks)}"
        embed = discord.Embed(
            title=title,
            description=chunk,
            color=COLORS["info"],
        )
        embed.set_footer(text=f"Update ID: {LATEST_UPDATE_ID}")
        embeds.append(embed)

    return embeds


def build_update_embed() -> discord.Embed:
    return build_update_embeds()[0]