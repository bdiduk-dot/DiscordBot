from __future__ import annotations

import discord

from config import COLORS

LATEST_UPDATE_ID = "2026-04-03-upd-2-2-easter-2026"
UPDATE_PING_ROLE_ID = 1486391112330379304
UPDATE_CHANNEL_ID = 1486032811809964243
UPDATE_TITLE = "🐣 Пасхальный ивент 2026"

LATEST_UPDATE_TEXT = """
> Временный ивент с собственной валютой, глобальными событиями и наградами. Прогресс и инвентарь сохраняются.

## 🥚 Валюта ивента
▸ Три типа яиц: **Обычное** 🥚 · **Расписное** 🎨 · **Золотое** ✨
▸ Яйца падают из привычных команд — ничего нового учить не нужно:
　— `/work` · `/crime` · `/slut` · `/fish` · `blackjack` · `/daily` · бизнес · аренда
▸ **Золотое яйцо** не падает напрямую — только через Золотого кролика, кейс и пруд

## 🔄 Обменник
▸ Апгрейд валюты только **вверх**:
　— `25` 🥚 → `1` 🎨
　— `10` 🎨 → `1` ✨
▸ Доступен в `/easter` во время ивента и после его окончания

## 🐇 Золотой кролик
▸ **Глобальное серверное событие** на 15 минут
▸ Усиливает дроп всех яиц и шанс пасхального сундука
▸ После рестарта бота событие **не спавнится заново** и не шлёт повторный анонс

## 🛒 Магазин ивента
▸ Открывается прямо в `/easter`, отдельной команды нет
▸ Доступно: пасхальный кейс, праздничная наживка, пасхальный фон, титул,
　корзина с яйцами, кроличья лампа, шоколадный фонтан, пропуск на Пруд, временные бизнесы

## 🏆 Коллекция
▸ Собери: `1` 🥚 + `1` 🎨 + `1` ✨ + пасхальный сундук + кроличий талисман
▸ Награда выдаётся **один раз** — повторная выдача заблокирована
▸ Закрыть коллекцию можно и после окончания ивента, пока есть нужные предметы

## 🎣 Пруд золотого кролика
▸ Доступен по **пропуску** из магазина
▸ Падают только ивентовые рыбы, яйца, сундуки и редкие находки
▸ После ивента спот скрывается, история вылова сохраняется

## 🏪 Временные бизнесы
▸ Во время ивента приносят яйца и деньги
▸ После окончания — **удаляются** и заменяются трофеем в инвентаре
　*(например: Трофей: Пасхальная Лавка 2026)*

## 📦 Инвентарь и жизнь после ивента
▸ Все ивентовые предметы хранятся в **обычном инвентаре**
▸ Трофеи, декор, рыбы, титулы и фоны остаются активными навсегда
▸ Яйца, сундуки и наживка уходят в подкатегорию **Архив 2026** в `/inventory`

## 💸 Выкуп яиц
▸ Работает только **после окончания** ивента, в фазе обмена:
　— 🥚 Обычное → `$300`
　— 🎨 Расписное → `$4,000`
　— ✨ Золотое → `$35,000`
▸ После закрытия фазы обмена выкуп **недоступен навсегда**

## 🐰 Команда `/easter`
▸ Главный экран ивента: таймер, статус кролика, твои яйца, прогресс коллекции
▸ Кнопки: `🛒 Магазин` · `🔄 Обменник` · `🏆 Коллекция`
"""


LATEST_UPDATE_TEXT += """

## fastFIX
▸ Урожай с огорода теперь можно продавать прямо через `/inventory`
▸ Во вкладке `🎒 Предметы` для урожая показывается цена продажи, а сам он продаётся отдельной кнопкой
▸ Магазин в `/easter` разбит по категориям, чтобы быстрее находить лут, профильные награды, мебель и бизнесы
▸ Внутри `/easter` у выбранного товара теперь есть понятное описание: что он делает, куда применяется и зачем нужен
▸ Пасхальный фон и титул теперь используют валидные ключи и реально открываются без поломки косметики
▸ В `/profile -> Кастомизация` снова можно переключать фон профиля, а активный фон сразу видно в списке
▸ Пасхальный фон теперь не только меняет цвет, но и ставит изображение в карточку профиля
▸ В обычный `/shop -> Кастомизация` добавлен фон **Пасхальная сакура** с превью, чтобы его можно было покупать как нормальную косметику
▸ `/shop` стал чище: главный экран больше не перегружен кучей кнопок, а навигация собрана в один аккуратный селект разделов
"""


def _split_update_text(text: str, limit: int = 3800) -> list[str]:
    lines = text.strip().splitlines()
    chunks: list[str] = []
    current: list[str] = []
    current_len = 0

    for line in lines:
        extra = len(line) + (1 if current else 0)
        if current and current_len + extra > limit:
            chunks.append("\n".join(current).strip())
            current = [line]
            current_len = len(line)
        else:
            current.append(line)
            current_len += extra

    if current:
        chunks.append("\n".join(current).strip())
    return chunks or [text[:limit]]


def build_update_embeds() -> list[discord.Embed]:
    chunks = _split_update_text(LATEST_UPDATE_TEXT)
    embeds: list[discord.Embed] = []

    for index, chunk in enumerate(chunks, start=1):
        title = UPDATE_TITLE if index == 1 else f"{UPDATE_TITLE} • продолжение {index}/{len(chunks)}"
        embed = discord.Embed(
            title=title,
            description=chunk,
            color=COLORS["easter"],
        )
        embed.set_footer(text=f"Update ID: {LATEST_UPDATE_ID}")
        embeds.append(embed)

    return embeds


def build_update_embed() -> discord.Embed:
    return build_update_embeds()[0]
